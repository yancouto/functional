initSidebarItems({"constant":[["MAX_CHILDREN","As a sort of guard against infinite loops, the default ‘len’-implementation only ever iterates this many times. This is so that broken ‘introspect_child’-implementations won’t case introspect_len to iterate forever."]],"enum":[["IntrospectionError","Ways in which introspection may fail"],["IntrospectorNavCommand","A command to navigate within an introspected object"],["SavefileError","This object represents an error in deserializing or serializing an item."],["Schema","The schema represents the save file format of your data structure. It is an AST (Abstract Syntax Tree) for consisting of various types of nodes in the savefile  format. Custom Serialize-implementations cannot add new types to this tree, but must reuse these existing ones.  See the various enum variants for more information:"],["SchemaPrimitive","A primitive is serialized as the little endian representation of its type, except for string, which is serialized as an usize length followed by the string in utf8."]],"fn":[["introspect_item","Create a default IntrospectItem with the given key and Introspect."],["load","Deserialize an instance of type T from the given `reader` . The current type of T in memory must be equal to `version`. The deserializer will use the actual protocol version in the file to do the deserialization."],["load_encrypted_file","Like [crate::load_file], except it expects the file to be an encrypted file previously stored using [crate::save_encrypted_file]."],["load_file","Like [crate::load] , except it deserializes from the given file in the filesystem. This is a pure convenience function."],["load_file_noschema","Like [crate::load_noschema] , except it deserializes from the given file in the filesystem. This is a pure convenience function."],["load_from_mem","Deserialize an instance of type T from the given u8 slice . The current type of T in memory must be equal to `version`. The deserializer will use the actual protocol version in the file to do the deserialization."],["load_noschema","Like [crate::load] , but used to open files saved without schema, by one of the _noschema versions of the save functions."],["save","Write the given `data` to the `writer`.  The current version of data must be `version`."],["save_compressed","Write the given `data` to the `writer`. Compresses data using ‘snappy’ compression format. The current version of data must be `version`. The resultant data can be loaded using the regular load-function (it autodetects if compressions was active or not)."],["save_encrypted_file","Like [crate::save_file], except encrypts the data with AES256, using the SHA256 hash of the password as key."],["save_file","Like [crate::save] , except it opens a file on the filesystem and writes the data to it. This is a pure convenience function."],["save_file_noschema","Like [crate::save_noschema] , except it opens a file on the filesystem and writes the data to it. This is a pure convenience function."],["save_noschema","Write the given `data` to the `writer`.  The current version of data must be `version`. Do this write without writing any schema to disk. As long as all the serializers and deserializers are correctly written, the schema is not necessary. Omitting the schema saves some space in the saved file, but means that any mistake in implementation of the  Serialize or Deserialize traits will cause hard-to-troubleshoot data corruption instead of a nice error message."],["save_to_mem","Serialize the given data and return as a Vec The current version of data must be `version`."]],"mod":[["prelude","The prelude contains all definitions thought to be needed by typical users of the library"]],"struct":[["Canary1","Useful zero-sized marker. It serializes to a magic value, and verifies this value on deserialization. Does not consume memory data structure. Useful to troubleshoot broken Serialize/Deserialize implementations."],["CryptoReader","A cryptographic stream wrapper. Wraps a plain dyn Read, and itself implements Read, decrypting and verifying all data read."],["CryptoWriter","A cryptographic stream wrapper. Wraps a plain dyn Write, and itself implements Write, encrypting all data written."],["Deserializer","Object from which bytes to be deserialized are read. This is basically just a wrapped `std::io::Read` object, the version number of the file being read, and the current version number of the data structures in memory."],["Field","A field is serialized according to its value. The name is just for diagnostics."],["IntrospectItemMutex","Type of single child of introspector for Mutex"],["IntrospectItemRwLock","Type of single child of introspector for RwLock"],["IntrospectItemSimple","Standard child for Introspect trait. Simply owned key string and reference to dyn Introspect"],["IntrospectItemStdMutex","Type of single child of introspector for std::sync::Mutex"],["IntrospectedElement","A node in the introspection tree"],["IntrospectedElementKey","Identifies an introspected element somewhere in the introspection tree of an object."],["IntrospectionFrame","All fields at a specific depth in the introspection tree"],["IntrospectionResult","An introspection tree. Note that each node in the tree can only have one expanded field, and thus at most one child (a bit of a boring ‘tree’ :-) )."],["Introspector","A helper which allows navigating an introspected object. It remembers a path down into the guts of the object."],["Removed","Helper struct which represents a field which has been removed"],["SchemaArray","An array is serialized by serializing its items one by one, without any padding. The dbg_name is just for diagnostics."],["SchemaEnum","An enum is serialized as its u8 variant discriminator followed by all the field for that variant. The name of each variant, as well as its order in  the enum (the discriminator), is significant."],["SchemaStruct","A struct is serialized by serializing its fields one by one, without any padding.  The dbg_name is just for diagnostics."],["Serializer","Object to which serialized data is to be written. This is basically just a wrapped `std::io::Write` object and a file protocol version number."],["Variant","An enum variant is serialized as its fields, one by one, without any padding."]],"trait":[["Deserialize","This trait must be implemented for all data structures you wish to be able to deserialize."],["Introspect","Gives the ability to look into an object, inspecting any children (fields)."],["IntrospectItem","A child of an object implementing Introspect. Is a key-value pair. The only reason this is not simply (String, &dyn Introspect) is that Mutex wouldn’t be introspectable in that case. Mutex needs something like (String, MutexGuard). By having this a trait, different types can have whatever reference holder needed (MutexGuard, RefMut etc)."],["ReprC","This is a marker trait for types which have an in-memory layout that is packed and therefore identical to the layout that savefile will use on disk. This means that types for which this trait is implemented can be serialized very quickly by just writing their raw bits to disc."],["Serialize","This trait must be implemented for all data structures you wish to be able to serialize. To actually serialize data: create a [Serializer], then call serialize on your data to save, giving the Serializer as an argument."],["WithSchema","This trait must be implemented by all data structures you wish to be able to save. It must encode the schema for the datastructure when saved using the given version number. When files are saved, the schema is encoded into the file. when loading, the schema is inspected to make sure that the load will safely succeed. This is only for increased safety, the file format does not in fact use the schema for any other purpose, the design is schema-less at the core, the schema is just an added layer of safety (which can be disabled)."]]});