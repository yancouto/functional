{
  name: 'basic',
  levels: [
    import 'anything.jsonnet',
    import 'identity.jsonnet',
    import 'two_arguments.jsonnet',
    import 'squaring.jsonnet',
    import 'indirection.jsonnet',
  ],
}
